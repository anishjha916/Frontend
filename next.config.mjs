/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['unsplash.com'], // Add the domains you want to allow
  },
  
};

export default nextConfig;
